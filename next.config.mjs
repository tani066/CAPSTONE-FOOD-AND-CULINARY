/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'img.spoonacular.com',
      'images.unsplash.com',
      'www.dorsey.edu',
    ],
  },
};

export default nextConfig;
